# Sample images

`results.png` and `arch.png` are auto-generated placeholders used by `examples/input_sample.json`.

They are produced lazily by `tools/image_utils.ensure_sample_images()`
(called from `cli.py --mock`). Replace with real figures when you have them.
